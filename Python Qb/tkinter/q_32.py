
import tkinter as tk

# Create a new Tkinter window
root = tk.Tk()

# Create the first button and add it to the window
button1 = tk.Button(root, text="Helo" )
button1.pack()

# Create the second button and add it to the window
button2 = tk.Button(root, text="Exit" )
button2.pack()
root.mainloop()
