# fliter a list using lambda functions

scores = [70,60,80,90,50]
filtered = []

for s in scores:
    if s > 70:
        filtered.append(s)


print(filtered)


# lamda fucntion that adds 10 to a given number as an argument , also creates a function that multiplies x with argument y and prints the result.

a = lambda x:x+10
print(a(5))
b = lambda x,y : x*y
print(b(2,5))

